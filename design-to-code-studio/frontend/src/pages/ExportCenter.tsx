import React from 'react';

/** Export configuration and execution page. */
const ExportCenter: React.FC = () => {
  return (
    <div className="page-export-center">
      {/* TODO: ExportCenter implementation */}
    </div>
  );
};

export default ExportCenter;
