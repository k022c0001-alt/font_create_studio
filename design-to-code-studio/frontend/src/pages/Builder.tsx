import React from 'react';

/** Visual site builder page with drag-and-drop canvas. */
const Builder: React.FC = () => {
  return (
    <div className="page-builder">
      {/* TODO: Builder implementation */}
    </div>
  );
};

export default Builder;
