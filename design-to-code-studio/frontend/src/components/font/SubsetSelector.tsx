import React from 'react';

/** Character subset selection UI (Phase 2). */
export const SubsetSelector: React.FC = () => {
  return <div className="subset-selector">{/* TODO: SubsetSelector implementation */}</div>;
};
