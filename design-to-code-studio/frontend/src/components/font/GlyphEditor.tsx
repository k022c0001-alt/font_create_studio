import React from 'react';

/** Glyph path editor (Phase 3 – future expansion). */
export const GlyphEditor: React.FC = () => {
  return <div className="glyph-editor">{/* TODO: GlyphEditor implementation */}</div>;
};
