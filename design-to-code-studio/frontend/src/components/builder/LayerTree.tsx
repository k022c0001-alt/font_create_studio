import React from 'react';

/** Layer hierarchy tree for the current canvas. */
export const LayerTree: React.FC = () => {
  return <div className="builder-layer-tree">{/* TODO: LayerTree implementation */}</div>;
};
