import React from 'react';

/** Property editor for the currently selected canvas element. */
export const PropertyPanel: React.FC = () => {
  return <div className="builder-property-panel">{/* TODO: PropertyPanel implementation */}</div>;
};
